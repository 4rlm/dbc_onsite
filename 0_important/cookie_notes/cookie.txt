completed version has been pushed.


COOKIE

classes:  cookie, oven
cookie: type, status, current bake time, max bake time.
oven: rack, on status, temp, timer

what interface?
cookie: bake - to update status, attr readers
oven: add batch, turn on, set timer, remove batch, turn off

- how will the classes interact with each other?

##############
require_relative ‘cookie’
class Oven
…..check batch, remove batch, select batch(type), bake_batch(time), turn_on
end

##############

class Cookie
… initialize(args)
type, status, bake max time, current bake time, 
end

###########

class ChocolateChip < Cookie

COOKIE_TYPE = ‘generic’
MAX_BAKE_TIME = 25

### Can use constants instead of initializing (@…).

end


class Cookie

def initialize
@….
@…
type = self.class::COOKIE_TYPE
@max_bake_time = self.class::MAX_BAKE_TIME

end


end


def update_status
with case statement…. ready, burned, 

end


turn_

on, turn_off, select_batch, 



put constants in an array





###########