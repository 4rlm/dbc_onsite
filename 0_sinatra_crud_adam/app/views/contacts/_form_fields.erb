<!-- # Duplicate info from new.erb and edit.erb forms goes in this partial. -->

<div class="form-group">
  <input name="contact[first_name]" type="text" value="<%= @contact.first_name %>" placeholder="first name" />
</div>

<div class="form-group">
  <input name="contact[last_name]" type="text" value="<%= @contact.last_name %>" placeholder="last name" />
</div>

<div class="form-group">
  <input name="contact[email]" type="text" value="<%= @contact.email %>" placeholder="email name" />
</div>
